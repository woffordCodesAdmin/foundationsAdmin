<%= form_with(model: dasher) do |form| %>
  <% if dasher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dasher.errors.count, "error") %> prohibited this dasher from being saved:</h2>

      <ul>
        <% dasher.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :index %>
    <%= form.text_field :index %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
